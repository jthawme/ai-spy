import * as React from 'react';
// import { action } from '@storybook/addon-actions';

import Card from './Card';

export default {
  title: 'Card',
};

export const standard = () => <Card>This is a card</Card>;
